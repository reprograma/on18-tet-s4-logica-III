//array é uma lista, coleção de dados de um mesmo contexto
//quando a gente não sabe a posição do array, a gente percorre o array com for of
let alunas = ["Adriana", "Julia", "Morgana"]

//objeto:
let prof = {
    nome: "Lilit",
    signo: "Aquario"
}

//métodos de iteração (percorrer array)
//método usa ponto. e parentes(), exemplo: numeros.find()
// find = usar para encontrar apenas 01

// métodos de iteração recebem função como argumento

//slice faz uma cópia (a partir do indice X vai retirar, aqui é o indice a partir da onde é para continuar)
//splice (a partir do indice X, aqui remove a partir de 0 ou X, + os n. que serão adicionados)
